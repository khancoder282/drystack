export const MEDIA_LIBRARY_DIRECTORY = 'assets';

// files/folders moved here (mirroring their original path) instead of being
// deleted immediately, so the File Manager can offer a restore action
export const TRASH_DIRECTORY = '.deleted';
