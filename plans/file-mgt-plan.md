Tại menu thêm group File management (File Managenent) - tận dụng api có sẵn chỉ làm giao diện thêm
[] - public (/asset), trang quản lý file 2 tab bài viết (hình ảnh, file theo cấu trúc bài viết)
[] - tất cả hình ảnh file ra, khi click vào ảnh có chế độ preview zoom, scrop ảnh, giảm dung lượng ảnh, có nút xoá trên đây - responsive theo kích thước ảnh
[] - có nút search, search theo name
[] - có nút upload, upload nhiều file lên thư mục tương ứng, ảnh bị trùng tên báo lỗi trong dialog confirm có 3 nút cancel (bỏ qua file đó và thực hiện các cái khác), replace (thay thế), upload (ghi thêm hash để tránh trùng tên) - xuất hiện checkbox để áp dụng chức năng cho tất cả Khi xử lý thành công mới post lên server 1 lần
[] - hiển thị dạng cây thư mục, file
[] - có thể chọn nhiều khi click vào footer của card (checkbox để chọn) - khi chọn nhiều có nút xoá
[] - chức năng xoá có confirm
[] - viết để tận dụng thành dialog cho fields.image và chọn ảnh cho content